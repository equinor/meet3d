import { appendConnectionHTMLList, addLocalTracksToConnection, addVideoStream, addScreenCapture, advertiseFile, dataChannelReceive } from '../public/main.js';

//import * as main from '../public/main.js';
test('name is square', () => {
    expect(main.ourID).toBe(0)
})
